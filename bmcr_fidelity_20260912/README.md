**修正BMCR-T实验准备状态**

用户已授权补做与修正H65一致的BMCR-T实验。本目录目前只保存准备证据，尚未提交修正BMCR训练作业、重新生成效用尺度或取得修正BMCR成绩。用户随后要求优先提交当前代码并生成外部讨论prompt，本次先完成该交付。

`initial_snapshot.json`是2026-09-12 21:48的只读记录：两个修正warm检查点均为20轮、2000更新，`fidelity_revision=lr_identity_crop_validity_v1`。条件网络的LayerNorm及隐藏层有正常非零参数，只有输出末层权重和偏置为零；不能把它描述成整个网络全零。

已核对的下一步顺序：在独立runs目录引用这些只读warm EMA → 从各自修正warm重新做训练集效用审计/尺度估计 → BMCR真实预检 → 各40轮联合训练 → 总25..60每5轮完整测试并单报60终点，匹配修正H65的选择规则。共享warm不重复训练，官方参考不重训；不能直接复用旧BMCR的scales或把旧BMCR终点续训当作新的同配方60轮结果。

该补实验应和DS3通过统一资源调度、已验证监督合同、任务效用及teacher接口联系起来。当前仍运行既有DS3控制器；本目录的存在不代表新控制器或新模型已经部署。
