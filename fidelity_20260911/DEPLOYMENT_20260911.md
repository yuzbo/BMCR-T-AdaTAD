# 完整单种子实验部署

截至2026-09-11 16:49（北京时间），S/B真实RTX4090预检均已通过，完整课程的预热作业S1285422、B1285423均已提交并处于PENDING(Priority)。没有正式训练更新或完整测试mAP。预检证据见[记录](GPU_PREFLIGHT_RESULTS.md)及[未舍入数值](GPU_PREFLIGHT_RESULTS.json)。

本轮固定seed3407；每骨干全部200视频训练20轮预热＋40轮联合，batch2、每轮100次更新。联合阶段总epoch25、30、35、40、45、50、55、60完整测试211视频/792窗口，按平均mAP最高的EMA选模，同分取较早轮次，同时单列第60轮结果。模型与训练代码仍是1c1552a的两项保真修正。

部署中发现，原调度在多个节点同样满载时会把两项预热固定到g0014。16:39该固定节点的Slurm预计启动日期为9月16日。现改为让Slurm从8个已验证4090节点中分配一台，排除gpu分区其余47个节点；原地更新了两个等待作业，没有取消重投。16:49两作业的ReqNodeList已清空，实际排除集合与目标一致，预计开始时间变为当天19:43:17。该值是随队列变化的调度估计，不是启动承诺。

集群的sinfo是汇总包装脚本，不返回请求的逐节点列表，因此生产调度从scontrol读取分区Nodes字段，再用其hostnames命令展开。4项专项测试通过，并用真实分区查询确认允许8个节点、排除47个节点。已有28项CPU测试及GPU预检未因这个纯调度改动重跑。队列控制器已重新加载新代码；最多2个自己的训练/预检＋1个测试/计时以及账户16作业上限保持不变。

节点约束的更新使用Slurm公开支持的[ReqNodeList/ExcNodeList接口](https://slurm.schedmd.com/scontrol.html)，后续提交使用[--exclude](https://slurm.schedmd.com/sbatch.html)。实际作业、检查点、指标和进度以远端本实验的deployment.json及runs目录为准。此前codex/publication的结果保留原样。
