# H65两项保真修正与60轮判断

用户授权：修正学习率分组、补回裁剪边界有效性，再判断60轮课程是否不足。全部200训练/211测试，S/B；不重训官方，不加均匀基线或160/40，不加入原时间网格、融合或BMCR新结构。

新工作树codex/h65-fidelity-20260911从已公开历史代码8a85290创建，和未验证mainline草稿隔离。本轮仅训练H65-C。K384、rank检测、global-TIA192、scout架构、辅助损失权重、20warm+40joint课程、AdamW/EMA与推理路径保持原方案。每骨干独立从识别预训练开始，新warm20/2000次更新、joint40/4000次更新。

最新用户指令改为按中间测试峰值选模型：联合阶段每5轮（总epoch25、30、35、40、45、50、55、60）用全部211视频评测EMA，按五阈值平均mAP最高选择；相等保留较早epoch。固定部署推理策略不改变。预热不作为候选，不增加独立uniform模型。所有课程仍完成60轮，不早停；同时保留第60轮终点，以区分峰值选模与旧固定终点结果。该选择明确使用测试集，不能称独立未见测试性能。所有候选检查点保留，公布完整曲线；选中候选和终点各统计计算/时延，不重复已有完整AP。

纠错一：按实际encoder/decoder分类器参数身份分组（388参数），18,816个attention内部投影使用trunk LR。纠错二：外部LoadFrames子类调用未修改上游crop，保持frame/GT/label与随机流，再附加按原始端点与截断端点比较的有效性（rtol0、atol1e-6）。仅转变和边界辅助监督排除假端点，动作性与检测GT不变。

CPU检查覆盖裁剪/RNG完全一致、端点与GT顺序、短窗、全部端点被截断、collate/config字段、实际scout参数分组。GPU预检每骨干用真实batch2做warm与full-joint两步、严格EMA重载和有限预测。两预检全部完成后才启动新完整训练。新warm/resume/terminal必须带fidelity_revision，拒绝旧错误配方checkpoint。

训练和测试由新独立收据调度；最多2个自己的训练/预检＋1个独立test（含pending），账户最多16作业并遵守GPU配额。失败不盲重试；旧控制器和他人作业不变。

结果比较：修正60轮S/B的测试峰值及第60轮终点对旧60轮H65-C；S另对历史90轮固定终点65.385724、已取得历史晚期曲线峰值65.65（四舍五入，见总80轮）及历史总60轮64.40作背景比较。历史课程为30+60，与20+40不相同。不能用新测试峰值与旧固定终点的差直接宣称纯模型增益；峰值和终点分开报告。新结果未出前不判断，仍有差距时也不直接归因训练长度。本轮不临时延长课程。
