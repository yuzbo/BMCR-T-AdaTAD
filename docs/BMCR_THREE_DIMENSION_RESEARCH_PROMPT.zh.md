请作为一名独立的时序动作检测（TAD）、条件计算和模型效率研究者，基于下面固定GitHub提交开展深入、发散的源码复审与跨领域文献调研。先定位仍未解决的科学问题，再研究其他领域怎样建模和解决同类问题，最后提出能够借鉴现有BMCR-T/H65成果、减少时序/空间/深度三维冗余并保持TAD性能的可验证方案。

本次需要研究与设计报告、实验建议和代码改动定位，不需要启动服务器作业。请保持独立判断，允许证伪我们的假设；既不要预设“完整clip已失败，只能逐帧选”，也不要预设“蒸馏或延长训练一定能恢复精度”。

把确定性纠错作为基础，同时完成跨领域机制研究和可证伪的新方案。对于尚未完成的实验，标明哪些判断需要等待它们；本次研究仍须深入讨论模型与任务本身，而不止重述LR修复、延长训练或通用蒸馏建议。

1. **固定源码和证据范围**

仓库：[yuzbo/BMCR-T-AdaTAD](https://github.com/yuzbo/BMCR-T-AdaTAD)。本次唯一主审查快照：

`239d098cd899936c35989fae6243c70259a85adb`

[固定源码树](https://github.com/yuzbo/BMCR-T-AdaTAD/tree/239d098cd899936c35989fae6243c70259a85adb)

请先读[当前实验上下文](https://github.com/yuzbo/BMCR-T-AdaTAD/blob/239d098cd899936c35989fae6243c70259a85adb/ds3_20260912/research_context_20260912/CONTEXT.zh.md)、[机器可读最新快照](https://github.com/yuzbo/BMCR-T-AdaTAD/blob/239d098cd899936c35989fae6243c70259a85adb/ds3_20260912/research_context_20260912/latest_snapshot.json)，再读[完整历史回顾](https://github.com/yuzbo/BMCR-T-AdaTAD/blob/239d098cd899936c35989fae6243c70259a85adb/ds3_20260912/retrospective_20260912/REPORT.zh.md)。

关键代码：`h65/full/{model,scout,utility,geometry,objectives,data,runtime}.py`、`h65/transport.py`、`h65/ds3/{model,auxiliary,losses,routes,runtime}.py`、`tools/{full_train,full_eval,ds3_train,ds3_eval}.py`。实验依据还包括`phase2_20260910/comparison.json`、`fidelity_20260911/{FINAL_COMPARISON,INTERMEDIATE_RESULTS,TRAINING_COMPLETION}.json`、`ds3_20260912/{PLAN,IMPLEMENTATION,RESULTS}.md`。

不要默认读取仓库默认分支，也不要把历史`04c35a3`项目当成当前代码。旧BMCR/H65完整训练结果对应`15280e5`旧配方；当前共享代码已包含LR/裁剪边界修复，不能倒推旧权重也已修好。历史H65-S65.385724%对应`42dba3f`的30+60轮、global-TIA192，不是后来的local-TIA8。纯净OpenTAD来源为`346d09d19e2091372cec48172dbe40f7b28bdee6`。

公开仓库不包含本项目的大权重和原始视频。请区分源码/记录核查、数学推导、toy验证和真实GPU/全测试复测；无法读取的文件或无法实施的实验明确说明，不编造已加载权重或已复测的结果。源码判断给出固定提交的文件、符号/行号；文献机制优先引用原论文和作者实现，并注明版本与证据边界。

2. **用户目标、已有成果与实验约束**

用户要求以已经取得成绩的BMCR-T为下一步改进锚点，同时借鉴修正H65；两者与密集训练、稀疏推理应形成连续研究链。要研究如何复用已学到的任务表示、边界/动作先验和条件效用，避免新路线从弱初始化无约束替换高质量状态后发生巨大性能下降。

“最好”的范围必须准确：在已完成的新模型B路线中，旧BMCR-T-B最高；S上修正H65高于旧BMCR；官方仍高于两者。

| 骨干 | 官方AdaTAD | 旧H65-C 60轮终点 | 旧BMCR-T 60轮终点 | 修正H65 |
|---|---:|---:|---:|---:|
| VideoMAE-S |69.0126%|62.8593%|63.1563%|63.4094%，第60轮峰值/终点|
| VideoMAE-B |71.1280%|66.6128%|67.3404%|67.1328%，第55轮峰值；第60轮66.9026%|

平均mAP是tIoU0.3/0.4/0.5/0.6/0.7的均值。旧BMCR平均增益仅S+0.2970pp、B+0.7276pp，S的@0.5/@0.7还下降。旧H65/BMCR的全模型矩阵/卷积FLOPs约为对应官方的51.55%/50.45%，但这些已训练模型的指定窗口时延更高，不能宣称已实现官方精度保持与推理加速。

全部主实验使用200训练视频、211测试视频/792窗口、seed3407。旧/修正H65与旧BMCR是识别预训练→20轮warm→40轮joint，冻结非Adapter VideoMAE，训练Adapter、检测器和scout；预热也训练ASFormer。官方仅复用提供的TAD权重测试，不重复官方完整训练。DS3使用提供的TAD教师与额外ImageNet MobileNetV3Small预训练，初始化、监督来源和课程均不同。

用户已授权补做同配方修正BMCR：复用修正warm20，重新做训练集效用尺度审计，再joint40和相同规则完整测试。它尚未开始，不能填写“修正BMCR”的新成绩。没有独立U384完整训练或160/40部署门槛；可以提出同一checkpoint上的均匀/随机推理控制。当前按用户要求采用中间完整测试峰值选EMA，须披露测试集选模。后续多种子或更严格泛化验证可以建议，但不能把附件中的比例、哈希脚本、审批标记或任意1pp门槛当成本项目已接受的指令。

3. **必须解释的新观测，尤其不要混淆零训练与训练后结果**

固定快照为2026-09-12 21:56：S D1已保存15/80轮、1500/8000更新；第10轮全测试仍运行。下表中的D1只已有第5轮EMA结果，不能当作80轮终局。

| S方案 | 已完成211视频平均mAP | 含义 |
|---|---:|---|
| D768G |69.0126%|官方global-TIA384密集参考|
| D768L |67.5344%|同权重改local-TIA8，仍全部48clips/12层，FLOPs未减少|
| Z16 |43.2835%|零新增训练，均匀选16完整clips＋物理中心插值|
| Z24 |56.1065%|零新增训练，均匀选24完整clips＋插值|
| Z36 |62.5804%|零新增训练，均匀选36完整clips＋插值|
| ZR24 |53.0022%|零新增训练，带覆盖锚点的确定种子随机24clip控制|
| T24A，epoch5 EMA |50.6458%|D1辅助模块训练500更新后的早期自适应24clip路线|

Z24的24不是24轮，也不是随机初始化的整个TAD模型。它相对local dense额外下降11.4279pp，global→local此前已下降1.4781pp。Z24的FLOPs约为官方50.6157%；本轮GPU模型平均/中位42.60/42.56ms，对应官方67.87/64.77ms，但全数据评测1229.65秒与官方1236.41秒接近。不能混用旧官方51.21ms与本轮封装/节点计时来计算速度提升。

请审视：Z0预算曲线及随机/均匀差异到底支持哪些结论？它们没有控制训练适配、teacher图、采样单元和检测表示，是否足以排除完整clip？第5轮EMA原始mAP比例是0.5064583163849076，不是0.6458。EMA0.999在500次更新后初值系数约0.6064；请研究初始化、EMA滞后与在线模型质量，不能把这个参数系数直接当作输出或mAP比例，也不能把低早期分数全部归因于EMA。

4. **先定位科学问题，再寻找机制；同时指出已经正确的部分**

请区分实际实现错误、模型合同变化、测量/解释错误和未验证的科学假设。重点回答：

- TAD中的“冗余”应如何定义？像素/特征相似、低注意力或低绝对梯度，何时不能代表可删除？能否用条件任务损失收益、信息可观测性、边界不确定性、覆盖约束及计算成本建立统一的帧/clip/空间/深度决策问题？帧级效用是否可直接相加成clip级效用？
- 性能下降来自多少观察缺失、分布变化、时间度量/邻接失配、全局上下文变化、优化/监督或系统实现？请区分已测到的差值与尚未拆分的原因，不把rank IoU反例或短动作直觉直接写成全部mAP损失的证明。
- 现有BMCR的S0局部交换监督与S1重新解码是否一致？条件效用、分量尺度、漏检惩罚和离散代理梯度哪些可靠、哪些要重新验证？当前标量训练损失的边际变化不等于真实mAP边际收益。
- 已有强模型的哪些能力可复用：修正warm、训练过的scout/边界先验、BMCR任务效用、预测teacher、dense全局上下文？哪些权重/表示不能直接迁移？请明确rank384与原detector768、global192/global384/local8的差异，不用宽松加载或形状相同代替语义兼容。

5. **开放讨论选择粒度，避免把问题预设成“16帧clip或逐帧”二选一**

这里候选stride=4个原视频帧；“16帧clip”实际是16个候选观察，不是16张连续原视频帧。官方768候选→48clips，每clip保留8个时间tubelet，形成native384，再插值到detector768。旧H65/BMCR的K384重新组装成24clips/native192，采用rank检测与NMS前回映；DS3-L保留原clip身份，恢复native384和原时间检测。

请比较逐帧、原tubelet、4/8/16候选观察micro-clip、重叠clip、自适应长短段、关键帧＋局部片段、全时间低成本状态＋稀疏深层更新等选择单位。哪些保留运动统计、tubelet相位和上下文，哪些改变预训练分布？应该在解码、RGB、patch embedding、attention还是FFN之前选择？选择层级分别能省去什么成本？训练时全量patch embedding但只深算部分时刻，是否比先删除RGB更适合保持TAD信息？

必须处理短动作、起止边界、多次同类动作、长背景、遮挡、多主体以及真实存在的部分/短窗口。区分有效观察数和物理填充槽位。原坐标恢复正确并不恢复被删除的语义；非均匀采样的时间间隔应如何进入特征、邻接、金字塔、GT指派和框回归？若要归因粒度，请提出能尽量固定预算、权重、训练历史、GT/检测轴与TIA设置的控制，明确仍不可分离的因素。

6. **空间与深度去冗余应保持可用状态和正确成本，而不是继续堆模块**

空间上研究规则降分辨率/多分辨率、前景局部细化、token merging/reconstruction、稀疏MLP、时空局部聚合、低秩更新等机制。不要默认分类token剪枝可直接迁移到TAD；原TIA依赖完整时空栅格，删除后如何恢复状态和位置？多主体和小动作证据怎样保留？

深度上研究逐视频/clip/时间token/空间token的早退出口、轻重路径、残差预测与自蒸馏。不同深度输出如何进入同一个检测特征接口？被跳过位置保存什么、由谁更新上下文？依据什么决定额外计算值得执行？训练参数冻结本身不算计算跳过。

参考算量仅供建模：S当前TIA约占矩阵MAC3.27%；仅后4层MLP保留75%/50%，在额外模块之前最多省原总量3.86%/7.72%；D8全时域出口约67.73% FLOPs、全时域160→128约58.89%，均不是对应精度或时延实测。请独立核算，不把三个保留率直接相乘。能否保留廉价全局时间通路，主要压缩attention/FFN等昂贵操作？如果保持global-TIA又跳部分clip贵算子，需要什么中间空间token状态和scatter机制？不能只改temporal_size后宣称等价。

7. **正面研究密集训练与稀疏推理的异构性，以及如何避免弱初始化导致的质量下降**

当前D1每次完整计算48clips/12层/全部tokens，冻结TAD教师和检测器，训练H0低分辨率预测、H8出口、后4层MLP近似与路由评分。H0视觉部分有ImageNet MobileNet预训练；新增输出/路由/出口/近似部分是新初始化。推理才按0/8/12层、空间比率执行：H0填未重算的位置，H8填浅出口，深层特征覆盖对应位置。

当前主要是dense feature fitting及梯度加权残差utility proxy；teacher的检测损失对teacher feature求梯度，用于标签，尚无真实混合稀疏学生图的检测损失训练。请检查这能否保持TAD性能，尤其对边界/少数动作、分类排序、特征尺度/归一化、路由和出口校准。local dense67.5344%是参照而非严格上限，但拟合它没有直接提供恢复global行为的监督。

请区分：Z0零新增训练；D1完整密集前向辅助学习；C2主干仍可完整前向，但辅助图见过缺失/虚拟稀疏的校准；J3稀疏路径联合适配。用户关心训练时保留完整观察与主干计算，但不要把它未经讨论地等同于“辅助训练绝不能模拟推理缺失”。哪种异构性在理论/经验上合理，哪种训练—推理分布失配需要显式解决？

请给出从已验证高质量基线逐步学习增量的初始化/课程方案：残差、恒等出口、重建基线、路由先验、teacher监督、混合预算、分阶段解冻或可靠性融合如何协调？当前混合输出不保证初始等于Z24或训练好的H65/BMCR；零logits也不自动产生均匀top-k。零输出末层不意味着整个网络全零，首步上游梯度为零也未必是错误。若少观察条件下无法保持某个基线的精确初始等价，请说明限制与实际替代，不能用推理先算完dense再mask来冒充省算。

研究如何把已有BMCR/H65知识纳入这条链：teacher预测在真实时间坐标对齐；训练过的scout提供边界/覆盖先验；clip/depth动作重新定义并校准条件效用。当前local clip独立性可能允许一次训练dense forward缓存浅/深特征，构造交换后主要重跑检测器生成标签。请判断适用条件、数值一致性和不能推广到global-TIA的情况。分类/定位蒸馏要匹配实际输出语义和学生梯度路径，保留GT监督；直接加TAD loss不会自动使hard top-k可微。

8. **跨领域调研必须围绕同类科学问题，允许提出本项目尚未考虑的方向**

至少覆盖视频TAD/动作识别/视频密集预测、图像检测/分割/局部细化、文本QA/span/事件抽取/长上下文条件计算、点云3D检测/分割。根据问题关联，可扩展流式音频事件/ASR、遥感变化或异常检测、主动感知、多速率不规则信号与自适应数值采样等领域。

对每类机制说明：原任务真正解决什么；哪些结构与TAD同构、哪些不同；如何建模冗余/成本/覆盖；被跳过信息和输出坐标如何保留；训练与推理是否异构；梯度、监督、teacher如何设计；真实计算在哪减少；为什么可能迁移、何时失败；相对已有方法的创新点在哪里。不要只排列论文摘要或只找名字含sparse的工作，也不要把分类正确率保持、训练省显存、proposal减少当成TAD少RGB推理精度保持的证据。

以下仅为检索入口，不是限定答案，更不是自动成立的迁移结论。请进一步补充相关研究和反例，并独立阅读原文：

- [AdaFocus V2](https://openaccess.thecvf.com/content/CVPR2022/papers/Wang_AdaFocus_V2_End-to-End_Training_of_Spatial_Dynamic_Networks_for_Video_CVPR_2022_paper.pdf)：视频动态空间计算、辅助监督与条件退出。
- [DToP](https://openaccess.thecvf.com/content/ICCV2023/papers/Tang_Dynamic_Token_Pruning_in_Plain_Vision_Transformers_for_Semantic_Segmentation_ICCV_2023_paper.pdf)：密集预测token早退与代表性上下文保留。
- [TR-BERT](https://aclanthology.org/2021.naacl-main.463.pdf)：面向QA等任务的逐token深度分配。
- [CoLT5](https://arxiv.org/abs/2303.09752)：所有token的轻路径与选择性重路径；须核对其训练条件，不能直接归类为纯D1。
- [RandLA-Net](https://openaccess.thecvf.com/content_CVPR_2020/papers/Hu_RandLA-Net_Efficient_Semantic_Segmentation_of_Large-Scale_Point_Clouds_CVPR_2020_paper.pdf)：随机点采样与局部几何聚合。
- [ETAD](https://arxiv.org/abs/2205.07134)：TAD训练梯度/提案采样；须区分训练效率与RGB推理省算。

9. **最终输出应形成可执行研究判断，而非无边界实验堆叠**

请依次交付：

（a）现状判断：已经可靠的执行/性能事实，仍存在的实现问题，最需要解释的科学问题；每项附证据和可证伪预测。回答“clip是否被现有证据排除”“BMCR与DS3怎样成为连续路线”“性能保证在什么条件下成立”。

（b）跨领域机制图谱：用问题—建模—解决机制—证据—迁移条件—失败方式组织，覆盖相似任务及反面证据，不以文献数量代替论证。

（c）经过发散后收敛的最多3条优先模型族：包含可利用现有高分BMCR/H65底座的渐进路线，以及能够检验clip/tubelet/全时间轻状态的替代路线。对每条给训练图、推理图、选择单位、时间坐标、轻/重状态、teacher来源、损失/梯度、初始化、预算与实际执行；说明哪些已有、哪些必须新实现、哪些不能直接复用。不要强制保留BMCR全部旧模块，也不要丢弃其已验证成果。

（d）最小判别实验顺序：先补同配方修正BMCR，再说明哪些诊断/消融能区分粒度、初始化、课程、teacher、时间图和稀疏适配因素；匹配或明确不匹配的有效观察、FLOPs、训练更新和teacher查询成本。不要直接安排所有方向的完整笛卡尔积。提出TAD平均/逐阈值mAP、短动作/边界/重复事件、覆盖/空洞、置信度与误差分析；说明单种子、测试峰值选择及后续不确定性评估的限制。

（e）系统测量和成本：实际执行的clip/block/token计数，完整/部分/短窗口，包含QK/AV的2MAC口径与未计操作；分开GPU骨干、完整GPU模型、解码/预处理/传输及窗口/视频NMS。报告路由、重建、teacher、缓存建立与存储成本，计时保留均值/中位/p95与波动。跨窗缓存必须验证上下文/位置条件，不能假设免费。区分已测、算子估算和收益预测。

（f）给Codex的实施交接：按固定源码列出最小改动文件/接口、关键张量形状和语义、应保留的已有checkpoint/监督、必要一致性测试、何种证据支持扩大预算实验或改路线。任何新精度/速度目标说明理由与验证方式，不编造已取得收益，不将未经用户接受的门槛变成强制审批。

请最后明确给出你的选择倾向：在现有200/211、VideoMAE-S/B和4090资源条件下，最值得先做什么、为什么；怎样同时减少三维冗余并保护TAD所依赖的视觉证据、时间几何和任务表示；哪些风险仍无法承诺消除。
