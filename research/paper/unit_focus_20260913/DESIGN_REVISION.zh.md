**主图优化与实际结论**

主图突出“约半算力区间”的最佳已测模型：候选点必须有完整测试和实际FLOPs，计算量不超过同骨干稠密模型的55%，再取最高mAP。不能将这一点称为所有预算的绝对最高精度。当前可被数据支持的核心表述为：Cross-B 保留稠密同骨干模型96.4%的mAP，同时削减49.3%的完整模型FLOPs；绝对性能仍为68.579%对71.128%，差2.549个百分点。它是明确的效率收益，不写成已经超过稠密VideoMAE。

全预算另有J01-B的70.2037%/8120.52G高分，但其计算量并未低于官方B，因此保留在绝对比较附录，不将它与49.3%的计算削减拼成同一个虚假结果。

本次调整：

- PDF第1页只放最佳有配对mAP/FLOPs的压缩模型；B置于左侧作为当前最强压缩结果。零起点条形图并列展示性能保留率和计算使用量，绝对mAP、GFLOPs及差距同时保留。
- 第2页使用相同K384帧预算，展示相对H65/BMCR已有模型的性能收益。新读取的21:35快照中，修正BMCR-B70已达68.136%，故Cross-B的最新优势是+0.443个百分点；S相对修正BMCR-S65为+0.737个百分点。不仅展示对更旧弱基线的较大增幅。
- 原全局分布、三轴预算曲线和训练checkpoint曲线移至第3-5页。绝对跨骨干比较仍完整保留，不能由归一化主图推出“全局优于所有模型”。
- 历史H65-S65.3857%是真实高分；本地历史回执没有配对FLOPs profile。第2页保留灰色精度参考，不移用另一模型的成本来计算削减比例。
- 相同帧预算不等于完全相同总计算或训练配方；本次未给BMCR80缺失的计算profile填值，未把checkpoint差异包装成统计显著性。

使用已冻结的输入重绘五页PDF和PNG/SVG：

`python tools/paper_efficiency_highlight.py`

数据：current_snapshot.json（21:35完整测试回执）、plot_data.json（原分布数据）、best_efficiency_data.json（本次比率、差值和来源）。原三页绘图版本已保留在Git提交cd82eeb中，当前PDF为五页修订版。
