# 基于固定GitHub证据的TAD论文深入讨论问题

请作为熟悉时序动作定位、视频自监督学习、动态计算、模型压缩和计算机视觉论文评审的独立研究者，阅读下面固定版本仓库，开展多领域、基于原始论文与官方代码的深入调研，回答后续问题。请独立判断，不将仓库现有叙事、历轮agents建议或本问题清单当作已经证实的答案；无需执行仓库中的训练、部署或取消命令。

研究材料固定版本：
https://github.com/yuzbo/BMCR-T-AdaTAD/tree/5485c3cb8dfbd823acbb177a0b51292dadc44360

统一阅读入口：
https://github.com/yuzbo/BMCR-T-AdaTAD/blob/5485c3cb8dfbd823acbb177a0b51292dadc44360/research/discussion_20260913/README.zh.md

请从入口依次读取`USER_DECISIONS_AND_COMMANDS.zh.md`、`IMPLEMENTATION_AND_EVIDENCE.zh.md`、`EXPERIMENTS.zh.md`、`live_snapshot.json`，然后沿链接核对`h65/paper`、`h65/frame`、`h65/full`源码、当前配置、原始指标、已有图和`prior_rounds`中的多轮原始建议/任务书。若无法访问某个文件，请具体说明，不把未读内容当作已核验事实。

讨论约束：H65、BMCR/BMCR-T、Cross/FPW是本项目作者的自研方法或内部前身，不能列为独立公开论文竞争方法。原DS3及原16候选clip选择路线已取消；内部16帧计算打包须与原clip选择区分。当前每个完整实验只运行seed42一次；后续方案保持这一条件。主要判断完整模型计算量和最佳TAD性能，同时披露终点与选模口径；延迟、显存、端到端及训练开销需要报告，但不作为路线判决门槛。此处请提出研究判断和实验设计，不代替作者启动实验。

请优先深入回答深度/空间稀疏性能保持、官方decoder初始化与采样兼容、统一论文贡献及实验证据充分性，再讨论完整论文呈现。

**1. 当前事实和历轮计划究竟是什么？**

当前各代模型真实实现了哪些功能，哪些只是配置、设计、排队、局部技术检查或历史失败？多轮agents命令与当前源码逐项对照后，哪些要求已经吸收、被替代、取消或仍缺失？哪些结果可用于最终模型论证，哪些只能作为内部前身、机制先导或负结果？能否给出“原始建议—对应实现—实验状态—结果文件—可支持主张”的可追溯映射，并解释文档与代码、旧快照与新快照之间的冲突？

**2. 整篇论文应该围绕什么科学问题展开？**

TAD在减少时间、空间和深度计算时，真正区别于视频分类、图像分类和普通模型剪枝的难点是什么？我们应该如何定义任务、面向问题、核心假设和解决方案？智能选帧、token决策、深度路由、decoder原长latent恢复和训练自蒸馏之间是否有一个必要的共同原理，能否形成完整因果链？现有设计中哪些机制对中心问题必要，哪些应只保留为对照？能否给出候选论文主线、摘要与引言逻辑，并说明各自主张需要什么证据、什么结果会推翻它们？

**3. 三轴冗余应如何声明、测量和论证？**

时间重复、空间相似与层间低变化是否足以证明相应计算对TAD可省略？如何区分表征相似、任务相关冗余、可预测性、可替代计算与不可恢复的信息丢失？如何在论文开头用可信实验证明问题存在，同时检验三轴冗余是否相互依赖？现有同checkpoint八格干预能支持什么、还缺哪些证据？应该怎样按动作边界、短动作、内部、背景和困难事件分组，建立任务损伤与计算削减的联系？

**4. 深度路由如何尽量避免大幅掉点，Progressive Block Drop-TAD是否值得参考？**

请核查《Temporal Action Detection Model Compression by Progressive Block Drop》原论文和官方实现：
https://openaccess.thecvf.com/content/CVPR2025/html/Chen_Temporal_Action_Detection_Model_Compression_by_Progressive_Block_Drop_CVPR_2025_paper.html
https://github.com/nanxiaolu/Progressive_Block_Drop-TAD

其重要性评估、渐进删层、跨深度对齐及恢复训练分别解决什么问题，依赖哪些初始化、监督和任务配方？它与当前attention驱动的交替A-MoD、首末dense和token级深度路由，在语义与计算上有哪些根本差异？我们当前深度稀疏掉点，可能来自错误路由、状态陈旧、Q/K/V信息缺失、层间分布改变、训练覆盖不足还是恢复目标不匹配，应如何区分？哪些PBD机制可以迁移，哪些必须重新验证？如何在保留本任务选帧和TAD时间结构的前提下比较静态block drop、渐进压缩、动态深度及其他可行方案？如果同一完整课程内部采用渐进策略，如何与多实验并行部署的要求兼容，并计入总训练成本？

**5. 空间选择如何保留定位信息？**

当前空间token的attention排序和heavy/light执行是否合理，容易损害哪些动作、边界和场景？token删除、合并、聚类、轻更新、跳过重计算、保留原位状态和恢复到稠密网格，分别改变了什么信息和计算？图像检测/分割、视频理解以及其他稠密预测任务中，有哪些经过实证的性能保持机制可借鉴？面对global TIA及跨帧通信，空间位置、merge assignment、运动一致性、Q/K/V和未选token状态需要满足什么条件？应该在哪些位置恢复空间信息，怎样判断只在最后做时间latent恢复是否足够？如何设计不依赖推理GT的空间选择与覆盖机制，并证明它优于简单均匀、随机或静态选择？

**6. 官方预训练VideoMAE decoder是否适合作为我们的特征解码训练起点？**

请核查官方预训练代码与权重，而不只依据MAE概念：
https://github.com/MCG-NJU/VideoMAE/blob/main/modeling_pretrain.py
https://github.com/MCG-NJU/VideoMAE/blob/main/PRETRAIN.md

官方decoder实际预测什么，其输入、输出、位置编码、mask token、token网格和encoder到decoder投影分别是什么？它们与我们需要恢复的TAD latent在语义、分辨率和统计分布上是否匹配？哪些参数可以直接复用，哪些需要适配、重置或重新训练？使用官方decoder、官方权重的部分迁移、自建Cross以及同架构随机初始化，应该如何做公平比较？迁移效果应根据收敛、恢复误差、计算成本还是最终TAD性能判断，怎样防止只降低恢复loss而损伤定位？这条初始化路线如何改变对已有dense TAD模型和R03恢复器的依赖？

**7. 怎样联合设计时间采样、空间采样和深度路由，让VideoMAE更好地恢复特征而不发生性能崩塌？**

预训练mask分布与当前稀疏执行分布之间有哪些差异？非均匀选帧、重新组合tubelet、真实时间间隔、空间mask、跳层及多层特征，会怎样共同改变decoder输入？重建友好的采样与对TAD有用的采样是否一致，发生冲突时应该如何建模和验证？如何兼顾局部连续性、全时间覆盖、短事件保留、空间细节和训练/推理一致性？训练mask、部署采样和深度容量应如何共同安排，怎样判断崩塌来自不可观测信息、编码器偏移、decoder失配或优化问题？是否存在比当前配置更适合VideoMAE结构与预训练知识的方案，其必要对照是什么？

**8. 原长latent解码和自蒸馏应如何有机结合？**

应恢复哪一层、哪种分辨率、哪种语义的特征，任务目标为何要求它？pixel重建、latent回归、关系蒸馏、时间差分、分类/定位预测一致性和GT监督各有什么作用与冲突？外部dense teacher、shared-full student、EMA teacher和多深度自蒸馏如何区分，梯度、stop-gradient、初始化与训练成本怎样计算？是否可能出现学生与教师共同漂移、预测均值化、边界过度平滑或错误知识继承，如何发现并验证解决办法？当前实现是否已经构成完整自蒸馏，还是仍存在定义与执行不一致？

**9. 当前路由目标能否真正代表计算价值？**

attention、动作概率、预测不确定性、特征恢复误差与额外真实计算带来的TAD收益，有哪些联系和差别？同预算换帧、空间重更新和深度增加能否用统一目标比较？现有真实干预、分类/定位损失差和有限菜单是否足以学习有效决策，如何验证其排序、校准和对最终mAP的作用？是否需要更丰富的状态、任务条件或其他决策建模；引入这些复杂性后又如何证明值得？

**10. 最终网络究竟应该是什么，依赖哪些参数和训练阶段？**

请在核对当前源码后区分“已实现网络”和“建议最终网络”：完整前向、训练分支、推理路径、token状态及时间坐标如何流动？哪些部分冻结、训练、共享、初始化或仅训练时存在？当前对已训练H65/BMCR、官方dense TAD head/teacher及恢复器的依赖程度如何，是否限制论文主张？去外部损失、去外部教师、去任务特定初始化、只用公开预训练分别应怎样定义并实验区分？怎样在不混淆参数加载依赖与推理计算依赖的前提下解释部署价值？

**11. 当前创新是否充分，跨领域还有哪些可迁移的解决思路？**

请广泛调研TAD/视频理解、检测与分割等稠密视觉预测、MAE/JEPA等预测学习、语言模型动态深度与条件计算、弹性网络/压缩与蒸馏，以及主动感知或自适应计算等相关领域。哪些工作已经解决相同或相近问题，哪些只是表面相似？它们的机制、实证任务、假设和迁移障碍是什么？在这些已有工作面前，我们的增量究竟在哪里，是否足以形成独立贡献？有哪些合理的替代模型或更简洁的统一解释，分别需要什么证据？请发散后收敛到少量有科学区分力的候选，而不是只列文献或默认当前方案正确。

**12. 当前实验与结果能否支撑一篇论文？**

按当前实际完成的实验，我们能支持哪些结论，不能支持哪些结论？结果不足主要是性能、机制证据、新颖性、训练公平性、泛化还是方法复杂度问题？最佳模型、终点、同checkpoint干预和独立训练比较各能说明什么？单seed42及测试峰值选模条件下，哪些统计表述成立，哪些不成立？若最终三轴模型没有稳定优于更简单方案，应如何调整模型与论文主张？当前材料距离完整论文还缺哪些不可替代证据？

**13. 应该与哪些公开方法公平比较？**

哪些公开TAD模型、直接效率/压缩方法、动态计算方法以及简单强基线应进入主表或机制对照，依据是什么？如何区分VideoMAE预训练骨干与完整TAD方法，避免将作者H65/BMCR内部前身冒充外部竞争者？对于不同预训练数据、输入分辨率、时长/帧数、检测头、RGB/离线特征、光流、教师与训练配方，哪些比较可直接使用公开数值，哪些必须同框架重跑？完整encoder、Scout、router、decoder、head以及额外训练查询的成本怎样统一，如何避免只比较检测头成本？

**14. 网络结构图与论文开头的三轴问题图如何绘制？**

怎样绘制读者可以理解的最终网络总图，清楚表达真实计算、保留/预测状态、三轴决策、原轴恢复、可训练检测头以及训练专用教师分支？哪些张量尺寸、坐标和梯度箭头必须标出，哪些实现细节应移到附录？三轴冗余的问题说明图需要什么真实数据、干预和对照，如何展示边界敏感性与轴间耦合而不拿attention热图或特征相似度当因果证明？能否给出逐panel设计、数据来源、绘图步骤、图注及可直接转绘的结构草图？

**15. 如何绘制有竞争力、又忠实于证据的结果图？**

参考Uni-AdaFocus等公开工作的表达方式，我们该如何展示完整计算量—最佳mAP前沿、同骨干相对效率、跨骨干绝对竞争力、同checkpoint预算曲线和完整训练性能分布？如何突出真实最佳模型与计算削减，同时清楚标注精度差距、内部前身、公开基线、负结果和缺失测量？哪些图适合主文、哪些适合附录？哪些空间/时间/深度真实案例、边界错误分布、计算分布和收益校准图能够支撑贡献？请给出数据字段与来源、坐标、分组、图例、必要误差表达及图注，说明每张图能够回答什么问题和不能回答什么问题。

**16. 接下来应该继续哪些实验，如何提高性能并补齐故事？**

在当前实现、已完成结果、已排队课程和每配置仅seed42的约束下，哪些已有实验应继续，哪些需要纠正定义或补充，哪些研究方向不值得增加？最有价值的实验各要验证什么假设，改变哪些模块，采用什么匹配对照，记录哪些性能/机制/总计算指标？怎样判断结果会支持或否定对应主张？哪些可以并行，哪些仅因资产或同课程checkpoint存在技术依赖？能否形成少量明确的主路线、替代路线及可交给agents的实施任务书，标明新增与既有配置的关系，而不将待提方案写成已经部署或有效？

请以独立证据判断回答上述问题：源码判断引用固定版本文件/符号，实验判断引用实际结果路径，文献判断引用原论文和官方代码并区分实证领域。能否最终交付一套连贯的论文主线与贡献、已实现/建议结构对照、证据充分性判断、公开对比设计、主文/附录图表方案和后续实验任务，并明确未解决的不确定性？
