**截至2026-09-13 13:42：当前实验说明什么，哪种方法最好**

当前最有价值的正向证据是：H65/BMCR选帧之后，用轻量模块恢复完整时间轴上的特征，可以在几乎不增加推理矩阵/卷积计算的情况下改善TAD结果。当前仍未实现大约减半计算同时达到官方全帧模型精度；三维联合裁剪仍存在明显精度损失。

全部mAP来自211视频、792窗口的五阈值平均值。GFLOPs是完整模型矩阵/卷积2MAC口径，包含attention、路由与恢复器。不同课程和checkpoint的表格比较说明当前已测表现，不自动构成算法的因果比较。

|比较范围|当前方法/配置|mAP (%)|GFLOPs|
|---|---|---:|---:|
|全部参考中的最高精度|官方AdaTAD-B，既有checkpoint|71.1280|8082.15|
|本任务新配置中的最高精度|联合B EMA5，K768，深度/空间均全计算|70.2037|8120.52|
|中等计算预算的重要参考|官方AdaTAD-S，既有checkpoint|69.0126|2347.89|
|K384选帧路线的B最高已测精度|BMCR-B锚点+Cross原时间轴恢复，EMA5|68.3052|4094.12|
|K384选帧路线的S最高已测精度|H65-S锚点+Cross原时间轴恢复，EMA20|64.5743|1226.36|

因此，如果问当前选帧主线的方法，答案是Cross全时间轴特征恢复；如果问不约束计算的绝对mAP，答案仍是官方AdaTAD-B。本任务70.20%的联合B控制使用全部768帧和全部深度/空间计算，计算量还略高于官方B，不能作为低计算成功案例。

按绝对计算量比较，官方S同时优于当前Cross-B的精度与计算量。Cross-S更适合作为当前低计算主线候选。两者相对各自官方骨干分别减少47.77%/49.34%的推理矩阵/卷积计算，仍分别低4.44/2.82个mAP百分点。这些是实际精度—计算量取舍，不应写成已经保持官方精度。

**已有支持的结论与边界**

1. 学习式时轴恢复有正向信号。R01零训练插值S63.8245%→Cross-S20 64.5743%，增加0.7497pp，计算量仅从1225.47增至1226.36G（约增加0.073%）。B从67.2721%→68.3052%，增加1.0331pp，计算量4093.09→4094.12G。新增训练与dense teacher获取有成本，不能将这种推理边际成本描述成训练免费。S的成对视频bootstrap95%区间[-.1252,1.5218]pp包含0，单种子下仍不能宣称稳定显著提升。

2. 当前证据更支持继续研究特征恢复，还不能确定Cross结构最优。同为第5轮，TCN-S64.2392%略高于Cross-S64.1172%；TCN20尚未完整测试，不能用Cross20直接判定其架构胜过TCN。当前Cross为自建恢复器，官方VideoMAE decoder复用分支只有加载/两更新预检通过，尚无正式性能结论。

3. 额外直接GT损失和EMA还不是主要已观测收益来源。Feature-only S5 64.0954%与GT+feature S5 64.1172%仅差.0217pp，配置和锚点核对匹配；两者都使用TAD教师，且特征loss仍有GT边界加权，不能解释为不需要GT或任务知识。Cross-S20 online64.5554%与EMA64.5743%仅差.0189pp，说明当前终点收益并非主要由EMA平滑产生。直接检测GT损失消融还要等待同20轮结果。

4. 时间、深度和空间的压缩损害会相互影响。同为第5轮，相对Cross，A-MoD50额外省S16.26%/B18.04%计算，却损失2.981/4.856pp；联合压缩省20.96%/23.73%，损失4.676/7.802pp。相同联合checkpoint八格中的时间×深度交互为S−1.2252pp、B−1.6719pp，低帧预算下再减深度更伤精度。当前不能把损失单独归因于attention路由，因为同20轮、均匀深度路由、静态深度和full-KV对照尚未全部完成。

5. 学习选帧的额外价值目前小于恢复模块的观测收益。同一Cross EMA5 checkpoint上，H65/BMCR选择相对均匀选择仅约+.19/+.21pp；随机选择明显更弱。这提示应认真保留均匀覆盖对照、验证条件计算收益，而不能只凭随机对照声称复杂学习路由不可替代。这些是推理时选择控制，并非独立训练的uniform模型比较。

6. 特征修复误差不宜未经验证就作为真实计算收益。32个训练窗口、约60个可行交换的干预诊断中，修复收益与真实RGB换帧收益相关性弱，联合模型部分相关性为负。它支持继续采用实际换帧效用监督和独立评估，而不是把“哪里重建误差大”直接等同于“在哪里增加真实计算最有用”。该诊断样本有限，不作为普遍不可能性结论。

7. 后期训练收益要按实际曲线判断。BMCR-S同80轮轨迹60→65从63.5664%升到63.8372%，增加.2709pp；Cross-S15→20仅+.0138pp。BMCR S/B训练已完成80轮，已测峰值S65 63.8372%、B60 67.7262%，终点80仍待测。当前不能把两个模型的训练需求概括成统一地继续加轮次。

最值得继续回答的科学问题是：在给定实际计算预算下，恢复器如何维持对TAD边界定位有用的完整时间状态，以及哪一部分真实计算能纠正对任务最有害的预测误差。当前优先补齐同20轮的Cross/TCN/feature-only和深度/空间对照，继续官方decoder随机/预训练配对、实际交换效用监督、混合预算和多种子验证。已注册路线按自身技术依赖和GPU额度继续，原DS3及原16候选clip选择保持取消。

延迟按用户要求单独报告：Cross-S20/B5模型平均104.35/172.40ms，来自不同4090分配；它不参与以上路线选择。当前9个评测仍Priority排队、无新失败，两控制器正常；1小时时限修改尚无开跑收益证据，不能声称提速成功。

本次依据：[13:42权威快照](interpretation_20260913_latest.json)、[已测结果manifest](monitor_20260913_1224/manifest.json)、[最近新增结果及曲线](monitor_20260913_1224/UPDATE.zh.md)。没有新完整测试，因此不重新生成相同的性能图。
