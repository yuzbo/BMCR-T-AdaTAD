**5485复审的完整实施与并行部署计划**

用户已授权实施和提交；附件原文完整保存在input_package及原ZIP中。复审基点5485c3c保留用于证据比较，实施工作树从3aef191建立，包含随后已修复的训练元数据错误及ANet恢复工具，避免重新引入已知故障。分支codex/support-review-20260914。

最新长消息覆盖：资源集中到10个P0完整答案，不按75项平均推进。原18项之外新增Full-V2 S/B、late/统一深度两个S控制和完整shared-full移除，共23项新增。C2/C3及C6/C7重复标签复用，不重训。PBD/Static改为空间容量1，作为Final-Simple；方法解释与资源排序详见STATUS.zh.md。

保留既有52个seed42课程与其recipe，新增EXPERIMENTS.json的18个课程及最新指令的5个配置，不生成额外训练笛卡尔积。新运行目录独立，复用现有单一调度所有权和合法配额。代码继承不是实验性能依赖，P/D/R各族并行；PBD及渐进容量只在本课程内部依赖自己的状态。

实施顺序是接口依赖，不是逐模型等待mAP：

1. 固定18项展开配置、参数角色、课程和比较合同；保存输入包与现场状态。
2. 实现depth-bypass轻残差、逐层曝光及TIA前后状态；同选帧冻结参考和诊断/损失。
3. 实现静态计算匹配、训练集贪心删层、分阶段恢复和原层ID映射；明确PBD-style适配与作者原配方区别。
4. 实现fresh Cross、MAE输入统计适配、预算条件FrameRouter与固定总查询预算；严格无外部查询与公共识别初始化控制。
5. 接通完整训练、课程续跑、EMA及各预算实测成本；每个新课程集成自身两步GPU检查。
6. 实现分析/图表入口、真实诊断、依赖和训练查询账本；有数据才生成性能点。
7. CPU与真实资产检查后部署独立运行版本，向统一持久队列登记全部23个新增课程及依赖评测/分析阶段，在现有额度内尽快提交。

全部10个P0完整课程为80轮，保存10/20/40/60/80；PBD删层时点为0/26/52，Static/PBD与Full-V1/V2/Uniform均完成相同80轮。40轮机制实验采用80轮LR前缀、10/20/40相同候选；识别初始化I01/I02为80轮。渐进D/S按包内1–5、6–10、11–20、21–40日程，80轮按比例展开。PBD总更新固定，训练集候选评价开销独立计数，不以测试mAP决定轮数。

新模型需要的状态/算子诊断独立于生产推理profile。最终判断始终使用完整模型实际FLOPs与匹配来源的最佳mAP，保留终点、负结果和全部候选。H65/BMCR/Cross是作者内部方法。未测值保持null；实现、已登记、Slurm提交、真实更新和完整结果分别报告。

范围：本轮18项明确规格及已有对照。merge/unmerge、闭环主动补算和额外大模型作为报告中的后续候选，不悄悄变成新增完整训练。PBD-style采用当前Adapter/可训练head及原层映射，不能将其写为官方LoRA配方faithful复现。
